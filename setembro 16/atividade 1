import os
import time

os.system("cls || clear" )

QUANTIDADE_NUMEROS = 3
lista_pares_positivos = []

def solicitar_numeros():
    for i in range(QUANTIDADE_NUMEROS ):
         while True:
              numeiro = int(input(f"Digite o {i+1}º"))

              if numeiro % 2 == 0 and numeiro > 0:
                   lista_pares_positivos
                   break
              else
                   print("")