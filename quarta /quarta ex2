import os
os.system("cls || clear" )

nota = float(input("Digite uma nota: "))

while True:
    nota = float(input("Digite uma nota"))

    if nota < 0 or nota > 10:
        print("\nA nota deve ser maior que 0 e maior que 10.")
    else:
        break    

print(f"Nota: {nota}")